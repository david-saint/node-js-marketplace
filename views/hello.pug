extends layout

include mixins/_storeCard

block content
  h2 Hi #{name} 💪!
    br
    | Sale ends in #{h.moment().endOf('day').fromNow()} ⏰
  .inner
    .stores
      each store in stores
        +storeCard(store)
